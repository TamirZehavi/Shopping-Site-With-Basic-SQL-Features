# Shopping-Site-With-Basic-SQL-Features
HTML loads SQL table contant
User can update rows, insert new items, delete existing items, refresh the list of items.
In addition there's a dynamic search bar
